# CoxPanel 公网订阅验收（脱敏）

- 完成时间：2026-09-08 05:05:09 UTC；project=`coxpanel-p1-acceptance-run-0907`，KUL。
- agent 发布为 `186.244.235.199:18443 -> 18443/tcp`。保留同一镜像、运行环境、state volume 和两条验收网络；原容器停止保留为 `coxpanel-p1-acceptance-run-0907-agent-1-before-public`。
- 独立公网订阅容器 `coxpanel-p1-acceptance-run-0907-public-subscription-1` 发布 `186.244.235.199:18173 -> 8080/tcp`，使用本目录 `public-subscription.conf`；只接入验收网络，read-only、cap-drop ALL、no-new-privileges，未改生产反代或防火墙。
- 管理 frontend 保持 `127.0.0.1:18173` 和 `10.14.14.30:18173`。公网 `/`、`/api/me`、`/api/nodes` 均 `404`；订阅 POST `403`；内网未认证 `/api/me` 为 `401`；订阅响应 `Cache-Control: no-store, private`。HEAD 由 backend 返回 `405`，订阅 GET 正常。
- 真实 owner API：先验证 `/api/me`，`PUT /api/nodes/1` 设置 `publicIp=186.244.235.199`，保留 `easyIp=10.14.14.30`；`PUT /api/topology/1` revision 3 → POST preview → 显式 POST deploy，均成功。
- 部署版本前缀 `1d6979e182ed`；agent 实际应用版本前缀 `874254f89b2a` 与 `/api/agent/config` 一致，节点 online。仅改客户端 server 地址，入站运行时配置未改变，版本哈希不变符合预期；并非省略 deploy。
- KUL 宿主连接公网 `186.244.235.199:18443` 成功。KUL curl 公网订阅 HTTP `200`，恰好 1 个 VLESS/Reality/Vision 节点，YAML `server=186.244.235.199`、`port=18443`，token 未轮换。
- 广州通过 `et-aliyun-gz` 首次 TCP 检查成功；后续 EasyTier SSH 不稳定，改用同机公网别名 `aliyun-gz`。测试流量走 `eth0` 默认公网路由，不走 EasyTier。
- 广州公网 curl 订阅 HTTP `200`，再次校验同一 server/port。临时 mihomo `v1.19.30` 从该公网订阅 provider 加载并显式选择唯一 VLESS 节点；`MATCH,PUBLIC-TEST`，没有 DIRECT 回退。
- 真实 VLESS + Reality + xtls-rprx-vision：代理请求 ipify HTTP `200`，出口严格等于 `186.244.235.199`；Google generate_204 HTTP `204`。两次 HTTPS 验证结果均 `0`，curl 连接地址均为本地临时代理而非直连目标。
- mihomo 官方固定版本压缩包 SHA-256：`db214c7a2517e63c150d123178d16d102e03a241ccdae4e5e07ffbe9cf56c6f9`，与官方资产 digest 一致。下载限速通过有界分段下载解决，未安装全局软件。
- 广州临时进程已停止；私有 mount namespace 内项目路径的 tmpfs 已删除并卸载，未留下远端持久文件，未操作生产进程。最终 SSH 验收退出 `0`。
- 完整公网 URL 仅保存于项目 `tmp/coxpanel-sub-url.txt`（覆盖）和 `tmp/coxpanel-sub-url-public.txt`，均 `0600` 且被 Git 忽略；本报告不含 URL、UUID、token 或 Reality 密钥。
- 项目 Go shared/agent/backend tests 与 build、frontend lint/build 共 8 项命令均退出 `0`。JSON 测试审计有 20 个明确 skip：17 个 opt-in DB、1 个 agent runtime、2 个本地 pinned-client 集成测试；不将这些 skip 声称为通过。frontend lint 10 warnings，build 1 个 chunk-size warning。
- 本次广州真实握手验收独立于上述被跳过的本地测试。原 `agent/agent` 未跟踪文件保持不动。
- 私密运行证据：`tmp/coxpanel-public-evidence.json`、`tmp/coxpanel-public-gz-evidence.json`、`tmp/coxpanel-public-validation/results.json`；简报 `tmp/coxpanel-public-report.md`。

## 限制与回滚

- 订阅获取入口目前为明文 HTTP；Reality 隧道通过验收不等于订阅下载具备 TLS。它仅用于本轮公网 acceptance，不能当作生产 HTTPS 入口；不为此修改生产服务或证书。
- 回滚前核对项目/service labels；只移除公网 gateway 与替换 agent，再将 `-before-public` 原 agent 恢复名称并启动。通过 API 恢复节点原 publicIp 后重新 save → preview → deploy。原节点非密钥字段与 draft 备份在项目 `tmp/coxpanel-public-rollback.json`。
- 未执行回滚或 down；当前公网验收服务继续运行。默认 `down.sh` 不覆盖额外公网 gateway/回滚容器，清理前需按标签单独处理这些资源，不执行全局 prune。
